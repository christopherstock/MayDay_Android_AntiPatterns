apply plugin: 'com.android.application'

android {

    signingConfigs {
        synapsy {
            keyAlias 'synapsy'
            keyPassword 'j3lw86cr3ms9i6ql5x8ubv5wb042lrr7'
            storeFile file('C:/Users/Stock/workspaces/android/SynapsyPicFood/_BAUSTELLE/certification/synapsy.keystore')
            storePassword 'heaven'
        }
    }
    compileSdkVersion 22
    buildToolsVersion '23.0.2'

    defaultConfig {
        applicationId "net.picfood"
        minSdkVersion 8
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.synapsy
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
}
